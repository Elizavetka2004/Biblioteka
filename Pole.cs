namespace Biblioteka
{
    public class Pole
    {
        public static double Kwadrat(double a)
        {
            double pole;
            pole = a * a;
            return pole;
        }
        public static double Kolo(double r)
        {
            return Math.PI * r * r;
        }
    }
}
